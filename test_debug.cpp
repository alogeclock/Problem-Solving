#include <bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);

  int N, M, X, Y;
  cin >> N >> M >> X >> Y;
  for (int i = 0; i < N; i++) {
    
  }
}